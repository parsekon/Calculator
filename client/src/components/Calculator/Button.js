const Button = ({ value }) => {
    return(
        <button className="h-14 bg-gray-200 p-2 m-2 rounded-[10px] flex items-center justify-center cursor-pointer ">{value}</button>
    )
}

export default Button;